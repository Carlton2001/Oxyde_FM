use std::fs::File;
use std::io::Read;
use base64;

fn main() {
    let test_file = "C:\\Users\\Biluf\\Documents\\Projects\\Biluf_FM_Clean\\README.md";
    println!("We can't test docx as we don't have one, but we can write the command for it.");
}
